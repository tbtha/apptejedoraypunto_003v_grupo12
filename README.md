# 📱 Nombre de tu Aplicación Móvil

¡Bienvenido/a al repositorio de la aplicación móvil de **Tejedora y punto*!

Esta aplicación es la interfaz de usuario mobile que consume la API de nuestro servicio backend.

***

## ⚠️ Prerrequisito Importante: Backend

**Antes de intentar ejecutar esta aplicación móvil, es OBLIGATORIO que descargues y ejecutes el programa backend asociado.**

La aplicación móvil depende completamente de la funcionalidad y los datos proporcionados por el servicio que corre en:

### $\rightarrow$ `tbtha/tejedoraypunto` en la rama `backend`

### Pasos a Seguir:

1.  **Clona o descarga** el repositorio del backend mencionado: `tbtha/tejedoraypunto` (asegúrate de estar en la rama `backend`).
2.  **Sigue las instrucciones** específicas en el **SWAGGER_README.md** de ese repositorio para su correcta instalación y ejecución.
3.  **Verifica** que el servidor backend esté **corriendo** y sea accesible (generalmente en `http://localhost:8082`) antes de proceder con la configuración de esta aplicación móvil.

***

## 🚀 Puesta en Marcha de la Aplicación Móvil

Una vez que el backend esté ejecutándose, puedes continuar con la configuración y ejecución de esta aplicación.

### Requisitos

* [Menciona herramientas específicas, ej: Node.js, npm/yarn, SDKs de plataforma, etc.]

### Instalación y Ejecución

1. Clona este repositorio

2. Ejecuta la aplicación desde Android Studio

***

## ⚙️ Configuración (Opcional)

Si necesitas cambiar la URL base del backend (por ejemplo, si no usas `localhost`), la configuración se encuentra en **[Ruta del archivo de configuración, ej: `src/config/api.ts`]**.

***

